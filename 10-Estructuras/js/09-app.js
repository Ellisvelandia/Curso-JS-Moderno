// const auth = false;
// const canPay = true;

// console.log(auth && canPay ? "yes auth" : "no auth");
// console.log(auth || canPay ? "yes auth" : "no auth");
// console.log(
//   auth ? (canPay ? "yes auth and can pay" : "yes auth, no can pay") : "no auth"
// );
